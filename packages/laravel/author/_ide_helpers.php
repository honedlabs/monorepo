<?php

declare(strict_types=1);

namespace Illuminate\Database\Schema {
    /**
     * @method void authorable(string $users = 'users') Add the authorable fields to the table.
     * @method void deleteable(string $users = 'users') Add the deleteable fields to the table.
     */
    class Blueprint {}
}
