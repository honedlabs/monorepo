<?php

it('retrieves binder from cache', function () {
    // Check cache first and foremost
});

it('retrieves binder by property', function () {
    // Checks property next -> only ever uses cache
});

// it('retrieves ')
