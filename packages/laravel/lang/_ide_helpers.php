<?php

declare(strict_types=1);

namespace Illuminate\Routing {
    /**
     * @method static \Illuminate\Routing\Router localize(string|array|callable $callback) Prefix the route with the locale, and set the app locale for the request.
     */
    class Router {}

    /**
     * @method static \Illuminate\Routing\RouteRegistrar localize(string|array|callable $callback) Prefix the route with the locale, and set the app locale for the request.
     */
    class RouteRegistrar {}
}

namespace Illuminate\Support\Facades {
    /**
     * @method static \Illuminate\Support\Facades\Route localize(string|array|callable $callback) Prefix the route with the locale, and set the app locale for the request.
     */
    class Route {}
}
